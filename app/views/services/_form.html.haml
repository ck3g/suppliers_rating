= simple_form_for service, html: { class: "form-horizontal" } do |form|
  = form.input :name, input_html: { class: "input-xlarge" }
  .form-actions
    = form.button :submit, class: "btn btn-primary", :"data-disable-with" => t(:wait)
